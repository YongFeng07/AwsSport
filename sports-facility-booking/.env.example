# Copy this file to .env and fill in real values. .env is git-ignored and
# never committed - only this .env.example (with blank placeholders) is
# tracked in the repo. See config.php for how each value is used.

# Database (leave blank to use the local/Docker defaults in config.php)
DB_HOST=
DB_USER=
DB_PASS=
DB_NAME=

# Google OAuth 2.0 (leave blank to hide Google login)
# Add GOOGLE_REDIRECT_URI exactly as an authorized redirect URI in Google Cloud.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/google_callback.php

# Photo storage (S3) - optional, leave all blank to keep saving photos to
# the local uploads/ folder
AWS_S3_BUCKET=
AWS_S3_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
